# Copia este archivo a .env y completa los valores reales.
PORT=3000
NODE_ENV=development
SENTRY_DSN=

# Las claves RSA se generan localmente con el script de la carpeta del proyecto.
# No subas private.pem ni public.pem al repositorio.
